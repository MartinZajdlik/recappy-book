name: Keep Render Awake

on:
  schedule:
    - cron: "*/7 * * * *"   # každých 7 minut
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS -m 10 https://recappy-book-official.onrender.com/actuator/health || true
