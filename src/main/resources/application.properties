spring.application.name=recappy-book

# Název SQLite souboru (v ko?eni projektu)
spring.datasource.url=jdbc:sqlite:recepty.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Jméno a heslo SQLite nepot?ebuje
spring.datasource.username=
spring.datasource.password=

# Dialekt (na?e t?ída)
spring.jpa.properties.hibernate.dialect=cz.martinzajdlik.recappy_book.config.SQLiteDialect

# Automatické vytvá?ení tabulek (vytvo?í tabulky podle entit)
spring.jpa.hibernate.ddl-auto=update

# Pro výpis SQL do konzole (a? vidí? co se d?je)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
